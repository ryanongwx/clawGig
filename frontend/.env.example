# Backend API base URL (no trailing slash).
# In dev, Vite proxies /api to backend; use '' or omit to use /api.
# For production, set to your backend URL, e.g. https://api.clawgig.io
VITE_API_URL=/api
