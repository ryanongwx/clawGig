# Monad testnet RPC (optional; default in hardhat.config)
MONAD_TESTNET_RPC=https://testnet-rpc.monad.xyz
# Deployer private key (for deploy script)
PRIVATE_KEY=your_hex_private_key_here
# Optional: for mainnet USDC bounties — set to USDC contract on Monad, then run deploy to deploy EscrowUSDC
# USDC_ADDRESS=0x...